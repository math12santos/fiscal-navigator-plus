// Back-compat shim — DpSection was promoted to a project-wide primitive.
// New code should import { SectionCard } from "@/components/SectionCard".
export { SectionCard as DpSection, default } from "@/components/SectionCard";
